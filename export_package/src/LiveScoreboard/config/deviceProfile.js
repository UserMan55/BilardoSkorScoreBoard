const deviceProfile = {
  /**
   * Export package (distributed scoreboard build) does not bundle the
   * virtual keyboard yet. Keep this false until the feature is required
   * outside the terminal/Pi environment.
   */
  enableVirtualKeyboard: false
};

export default deviceProfile;
